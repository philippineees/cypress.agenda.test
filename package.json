{
  "name": "teste_automatizado",
  "version": "1.0.0",
  "description": "projeto para estudo de automação utilizando cypress",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Eder de Franca Lemes",
  "license": "ISC",
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^18.0.6",
    "@cypress/browserify-preprocessor": "^3.0.2",
    "cypress": "^13.2.0"
  },
  "cypress-cucumber-preprocessor": {
    "stepDefinitions": "cypress/e2e/tests/*/steps/**/**.steps.js",
    "filterSpecs": true,
    "omitFiltered": true
  }
}
